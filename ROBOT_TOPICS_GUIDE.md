# 🤖 두산 로봇(A0912) ROS 2 주요 토픽 쉬운 설명서

두산 협동로봇과 컴퓨터(ROS 2)가 연결되었을 때, 둘 사이에서 오고 가는 수많은 **"대화(토픽, Topic)"**들이 있습니다.  
이 문서에서는 각 토픽이 **무엇을 의미하고 왜 필요한지** 중학생도 이해할 수 있게 비유를 들어 쉽게 정리했습니다.

---

## 1. 토픽(Topic)이란 무엇인가요? 📢
ROS 2에서 **토픽**은 **"유튜브 채널"**이나 **"라디오 방송"**과 같습니다.
* **발행(Publish)**: 로봇이나 프로그램이 자기 상태나 명령을 방송합니다.
* **구독(Subscribe)**: 필요한 프로그램이 그 방송을 듣고 동작합니다.

---

## 2. 가장 중요한 핵심 토픽 4가지 ⭐

### ① `/joint_states` (관절 상태 알리미)
* **비유**: *"선생님, 저 지금 오른팔 90도, 손목 30도 꺾고 서 있어요!"*
* **내용**: 로봇의 6개 관절(Joint 1~6)의 **현재 각도(Position)**와 **움직이는 속도(Velocity)**를 실시간으로 계속 방송합니다.
* **확인해보기**:
  ```bash
  ros2 topic echo /joint_states
  ```

---

### ② `/tf` 및 `/tf_static` (3D 뼈대 위치 지도)
* **비유**: *"어깨에서 30cm 위에 팔꿈치가 있고, 팔꿈치 끝에서 25cm 앞에 손가락이 있어요."*
* **내용**: 로봇 바닥(Base)부터 끝단 툴(Gripper/Flange)까지, 각 관절 부위가 3차원 공간에서 **정확히 어디 위치(X, Y, Z)에 어떤 각도로 놓여있는지** 실시간 변환 좌표를 알려줍니다.
* **용도**: 3D 화면(RViz)에 로봇 모습을 똑같이 띄울 때 필수입니다.

---

### ③ `/robot_description` (로봇 설계도)
* **비유**: *"로봇 레고 조립 설명서"*
* **내용**: 두산 A0912 로봇의 팔 길이, 관절 회전 한계, 3D 외형 모델 파일(URDF) 정보가 통째로 들어있습니다.
* **용도**: 로봇이 자기 팔이 얼마나 긴지 알아야 벽이나 바닥에 부딪히지 않게 계산할 수 있습니다.

---

### ④ `/error` & `/robot_disconnection` (비상 경보벨 🚨)
* **비유**: *"삐-! 충돌 위험 감지!", "랜선이 빠졌어요!"*
* **내용**: 로봇에 이상이 생기거나 컨트롤러와 랜선 연결이 끊어졌을 때 즉시 경고 신호를 보냅니다.

---

## 3. 역할별 토픽 한눈에 보기 📋

| 토픽 이름 | 한 줄 설명 | 이해를 돕는 비유 |
| :--- | :--- | :--- |
| **`/joint_states`** | 6개 관절의 현재 회전 각도/속도 | *"지금 내 관절 각도는 몇 도야!"* |
| **`/tf` / `/tf_static`** | 각 관절 사이의 3D 공간 좌표 위치 | 3D 공간 속 로봇의 뼈대 위치 |
| **`/robot_description`** | 로봇의 크기, 형태, 한계값(URDF) | 로봇의 신체 스펙 및 설명서 |
| **`/error`** | 제어기 에러 발생 알림 | 응급 경고등 |
| **`/robot_disconnection`** | 로봇 연결 끊김 감지 | 통신 끊김 알림 벨 |
| **`/display_planned_path`** | MoveIt이 계산한 미래의 이동 경로 | 내비게이션의 추천 이동선 |
| **`/dsr_moveit_controller/...`** | MoveIt 궤적 명령을 로봇에 전달 | 머리(AI/경로생성)가 팔에 보내는 신경 신호 |
| **`/dsr_controller2/..._stream`**| 1초에 수백 번 실시간 속도/위치 주입 | 게임 조이스틱으로 직접 실시간 조종하기 |
| **`/planning_scene`** | 로봇 주변의 장애물 및 작업대 정보 | 로봇이 바라보는 주변 환경 지도 |

---

## 4. 터미널에서 자주 쓰는 유용한 명령어

```bash
# 1. 지금 방송 중인 모든 토픽 이름 보기
ros2 topic list

# 2. 특정 토픽에서 어떤 데이터가 나오는지 직접 눈으로 보기
ros2 topic echo /joint_states

# 3. 토픽이 1초에 몇 번 방송되는지(주파수/Hz) 확인하기
ros2 topic hz /joint_states

# 4. 토픽의 상세 정보(데이터 타입, 방송자 수) 확인하기
ros2 topic info /joint_states
```
